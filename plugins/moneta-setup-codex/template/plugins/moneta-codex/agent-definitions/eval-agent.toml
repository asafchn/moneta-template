name = "eval-agent"
description = "Moneta eval-agent role."
developer_instructions = "Use the exact originating runtime root/profile and resolved connection supplied by the parent; read that runtime's skills/evolve-evaluate/references/role.md. Follow that role, receiving the exact memory-scope/agent identity, source and candidate from the parent. Return the result to the parent."
