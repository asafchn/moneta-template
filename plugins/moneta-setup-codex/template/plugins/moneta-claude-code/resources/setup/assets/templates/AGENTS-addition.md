<!-- moneta:start -->
Before acting, invoke $<runtime-plugin>:knowledge-search against the originating runtime profile and its private local graph connection. Use no agent-slug for general; use an explicit agent-slug only when this role was enrolled into that area. Pass the same selection to evolution and delegated review. Read/write only that area, with no cross-agent or general fallback. Read its indexes/schema-definition and indexes/node-index, form a task-specific query, then find headers, walk relevant direct relations and read selected bodies. Repeat when new needs emerge.

When asked to evolve, invoke $<runtime-plugin>:evolve. It selects setup, one-message learning or run analysis from explicit intent and context, asking only when the choice is unclear. Keep the same explicit agent-slug; context never changes memory selection. Delegate analysis through $<runtime-plugin>:evolve-agent and review to a native child following the installed evolve-evaluate skill's references/role.md and $<runtime-plugin>:evolve-evaluate. Resolve that role relative to the installed plugin, not this project's agents directory. Use existing deterministic tools and recorded metrics for review; keep judgments distinct. Propose changes through the configured GitHub/GitLab repository's PR/MR.

Retrieve sourced domain-knowledge nodes through knowledge-search; verify source freshness when needed. Evaluation criteria/results use their separate skill. Human merge controls activation.
<!-- moneta:end -->

Substitute <runtime-plugin> with the actual generated identity before installing this role. Pass its runtime root/profile and resolved connection to every child; skill names in another instance never select this memory.
