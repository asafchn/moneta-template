---
name: evolve-agent
description: Analyze a captured agent run and draft evidence-supported knowledge changes.
---

Read the role instructions bundled at `skills/evolve-agent/references/role.md` relative to the installed plugin root. Follow that role and return its analysis to the parent.

Use the originating runtime root/profile and resolved connection supplied by the parent. Resolve skill and role instructions only from that runtime, retaining the explicit agent-slug through every call.
