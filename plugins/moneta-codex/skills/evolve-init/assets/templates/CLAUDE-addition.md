<!-- moneta:start -->
Before acting, invoke /moneta:knowledge-search against the local graph configured in .moneta.md. Use no agent-slug for general; use an explicit agent-slug only when this role was enrolled into that area. Pass the same selection to evolution and delegated review. Read/write only that area, with no cross-agent or general fallback. Read its indexes/schema-definition and indexes/node-index, form a task-specific query, then find headers, walk relevant direct relations and read selected bodies. Repeat when new needs emerge.

When asked to evolve a current run or captured transcript, invoke /moneta:evolve. Use the plugin's evolve-agent for analysis and eval-agent with /moneta:evolve-evaluate for review. Use existing deterministic tools and recorded metrics; keep judgments distinct. Propose changes through the configured GitHub/GitLab repository's PR/MR.

Retrieve sourced domain-knowledge nodes through knowledge-search; verify source freshness when needed. Evaluation criteria/results use their separate skill. Human merge controls activation.
<!-- moneta:end -->
