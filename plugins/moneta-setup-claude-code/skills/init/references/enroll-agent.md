# Initialize one memory area

1. Retain [memory selection](../../../template/plugins/moneta-claude-code/skills/knowledge-search/references/memory-scope.md). No agent-slug selects memory/general/; an explicit slug selects only memory/<slug>/. Inspect that area's existing state and reuse any pending setup review. Already initialized, unchanged content needs no new proposal.
2. Create a candidate checkout from the reviewed base. For a missing area, copy the complete installed `skills/init/assets/graph/general/` directory directly to the selected path with the host's filesystem copy primitive. Resolve source/destination absolutely first. Work from the candidate directory; installed plugin files are read-only inputs. Compare relative file names and SHA-256 hashes with the source before customizing. A partial or interrupted copy resumes missing files; preserve conflicting files and ask about material collisions. Do not reconstruct files from tool-output diffs or generated patch text.
3. The bundled graph is ready for general use and contains no example role enrollments. Preserve its indexes and relations when unchanged. If a role was explicitly supplied, distill its actual responsibilities with source pointers, add applicable mirrored edges and rebuild only affected indexes. User-specific guidance and domain facts require evidence. Other areas remain untouched.
4. Invoke evolve-evaluate for the exact candidate and selected area, with private evaluation paths and actor identity (unassigned if absent). Use [offline validation](../../../template/plugins/moneta-claude-code/skills/evolve-evaluate/references/offline-validation.md); inspect its exit status before publishing. Serialize assessment frontmatter with an available YAML/JSON serializer and parse it back against its schema.
5. Follow [hosted review](../../../template/plugins/moneta-claude-code/skills/evolve/references/hosted-review.md). The setup PR/MR contains only the graph and requested README changes. Reuse the area's pending review; future retrieval waits for human merge.

Completion: one assessed setup proposal or a verified unchanged area. Return the review URL and activation state in plain language; keep validation detail in the review.
