# Graph format

The authoritative domain format is bundled with init: [schema definition](skills/init/assets/graph/general/indexes/schema-definition.md). Each domain has its own indexes, node-type folders and JSON schemas.
