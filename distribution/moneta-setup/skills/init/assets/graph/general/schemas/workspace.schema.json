{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://moneta.invalid/schemas/workspace.schema.json",
  "title": "Workspace connection frontmatter",
  "type": "object",
  "properties": {
    "repository-url": {
      "type": "string",
      "minLength": 1,
      "pattern": "\\S"
    },
    "hosting": {
      "enum": [
        "auto",
        "gitlab",
        "github"
      ]
    },
    "checkout": {
      "type": "string",
      "minLength": 1,
      "pattern": "\\S"
    },
    "graph-root": {
      "type": "string",
      "minLength": 1,
      "pattern": "\\S",
      "description": "The personal Moneta repository memory/ directory. Each direct child is general or an agent slug."
    },
    "evaluation-root": {
      "type": "string",
      "minLength": 1,
      "pattern": "\\S"
    },
    "domain-source": {
      "oneOf": [
        {
          "type": "null"
        },
        {
          "type": "object",
          "properties": {
            "location": {
              "type": "string",
              "minLength": 1,
              "pattern": "\\S"
            },
            "access": {
              "type": "string",
              "minLength": 1,
              "pattern": "\\S"
            }
          },
          "required": [
            "location",
            "access"
          ],
          "additionalProperties": false
        }
      ]
    },
    "base-branch": {
      "type": "string",
      "minLength": 1,
      "pattern": "\\S",
      "description": "Reviewed branch used for ordinary retrieval; discovered from the supplied repository."
    },
    "bindings": {
      "type": "array",
      "minItems": 0,
      "uniqueItems": true,
      "items": {
        "type": "object",
        "properties": {
          "agent": {
            "type": "string",
            "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$"
          },
          "memory-scope": {
            "type": "string",
            "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
            "description": "Enrollment location only; an invocation without agent-slug still selects general."
          },
          "role-file": {
            "type": "string",
            "minLength": 1
          }
        },
        "required": [
          "agent",
          "memory-scope",
          "role-file"
        ],
        "additionalProperties": false
      }
    }
  },
  "required": [
    "repository-url",
    "hosting",
    "checkout",
    "graph-root",
    "evaluation-root",
    "domain-source",
    "base-branch",
    "bindings"
  ],
  "additionalProperties": false
}
